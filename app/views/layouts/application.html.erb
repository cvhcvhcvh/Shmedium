<!DOCTYPE html>
<html>
  <head>
    <title>Shmedium</title>

    <meta property="og:title" content="Shmedium" />
    <meta property='og:image' content="<%= image_url('shmedium.gif') %>"/>
    <meta property="og:description"
      content="Shmedium is a clone of Medium using Rails and React that allows users to create an account, publish stories, and read stories by way of following and interacting with other users."
    />
    <meta property="og:url"
      content="https://shmedium-app.herokuapp.com/#/"
    />

    <%= favicon_link_tag asset_path('logo.png') %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <script type="text/javascript">
      window.quote= "<%= image_url('quote.png')%>"
      window.logo= "<%= image_url('aalogo.png')%>"
      window.home= "<%= image_url('logo.png')%>"
      window.login= "<%= image_url('login.png')%>"
      window.github= "<%= image_url('github.png')%>"
      window.linkedin= "<%= image_url('linkedin.png')%>"

    </script>
    <% if current_user %>
      <script>
          window.current_user = {
              "id": <%= current_user.id %>,
              "email": "<%= current_user.email %>", 
              "username": "<%= current_user.username %>"
          }
      </script>
    <% end %>
  <script src="https://kit.fontawesome.com/a23a6114ea.js" crossorigin="anonymous"></script>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100&display=swap" rel="stylesheet">
  

  </head>

  <body>
  
    <%= yield %>
  </body>
</html>
